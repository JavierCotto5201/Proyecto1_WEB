import React from "react";
import ReactDOM from "react-dom";
import "./index.css";

const StartComponent = () => {
  return <h1> New Spotify</h1>;
};

ReactDOM.render(<StartComponent />, document.getElementById("root"));